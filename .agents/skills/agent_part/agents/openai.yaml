interface:
  display_name: "Agent Part"
  short_description: "Repo-specific patterns and workflows for agent_part"
  default_prompt: "Use the agent_part repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true